spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fundamentals?TimeZone=Europe/Kyiv
    username: fundamentals_training
    password: fundamentals_1234
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    format-sql: true
  flyway:
    enabled: true